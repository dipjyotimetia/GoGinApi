apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gogin.fullname" . }}-test-connection"
  labels:
    {{- include "gogin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gogin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
